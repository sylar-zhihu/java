启动服务器
用浏览器访问 
打印出消息的请求
加入多线程,获取uri
http://localhost:8080/index.html
加入对html页面的响应
加入servlet 去响应业务请求
加入web.xml
加入servlet 接口
http://localhost:8080/login
加入response 接口,用来更容易的会写数据
加入request对象，可以接受访问参数
访问：
http://localhost:8080/UserLogin.html
填好参数，提交表单 后台打印


