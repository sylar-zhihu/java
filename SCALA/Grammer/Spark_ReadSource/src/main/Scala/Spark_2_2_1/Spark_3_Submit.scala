package Spark_2_2_1

import org.apache.spark.deploy.SparkSubmit
import org.apache.spark.deploy.rest.RestSubmissionClient
import org.apache.spark.deploy.Client

object Spark_3_Submit {

  def main(args: Array[String]): Unit = {
    // 应用程序提交入口类
//    SparkSubmit.main()
    // 进入这一行 case SparkSubmitAction.SUBMIT => submit(appArgs)

    // 调用 doRunMain()

    // 调用 runMain()


    // standAlone 模式
//    Client.main()
//    RestSubmissionClient.main()

  }
}
